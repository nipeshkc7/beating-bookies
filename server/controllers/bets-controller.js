"use strict";

const user_service = require('../services/bets-service');


async function addBet(userId, bet) {
    res.status('200');
}

async function deleteBet(userId, bet) {
    res.status('200');
}

async function updateBet(userId, bet) {
    res.status('200');
}

async function getBet(betId) {
    res.status('200');
}

async function getAll() {
    res.status('200');
}

module.exports = {
    addBet,
    deleteBet,
    updateBet,
    getBet,
    getAll,
}